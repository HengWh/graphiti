# 核心依赖项 - 运行 Graphiti 所需的最小依赖
pydantic>=2.11.5
neo4j>=5.26.0
diskcache>=5.6.3
openai>=1.53.0
tenacity>=9.0.0
numpy>=1.0.0
python-dotenv>=1.0.1

# 可选依赖项（取消注释以启用对应功能）
# anthropic>=0.49.0          # 用于 Anthropic Claude 模型
# groq>=0.2.0               # 用于 Groq 模型
google-genai>=1.8.0       # 用于 Google Gemini 模型
# falkordb>=1.1.2,<2.0.0    # 用于 FalkorDB 数据库（替代 Neo4j） 